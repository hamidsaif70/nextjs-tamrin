import { css, jsx } from "@emotion/css";
import Input from "../Input";

const TextInput = ({  placeholder, width }) => {
    return (
        <Input type='text' placeholder={placehoder} width={width} ></Input>
    );
};
export default TextInput